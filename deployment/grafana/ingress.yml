apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: grafana-app-ingress
  namespace: monitoring
spec:
  ingressClassName: nginx
  rules:
  - host: grafana.tcgoidc.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: grafana
            port:
              number: 3000
